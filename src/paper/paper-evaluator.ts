export class PaperEvaluator {
  async evaluateDuePositions(): Promise<void> {
    // Version 1 proxy evaluation hook:
    // compare current Meteora pool TVL, volume, blacklist state, and fee/TVL at 1h, 4h, and 24h.
  }
}
